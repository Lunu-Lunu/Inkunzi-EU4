country_decisions = {
	Survian_union = { # might be changed to an end game tag insted of diffrent name
		major = yes
		potential = {
			tag = B16
			has_global_flag = sz_union_available
			
		}
		allow = {
			OR = {
				NOT = { exists = B14 }
				NOT = { exists = B16 }
			}
			owns = 2343
			owns = 2419
			owns = 2381
			owns = 100
			#NOT = { }
		}
		effect = {
			change_tag = F03
		}
	}
}